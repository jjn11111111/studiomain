{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // This maps any path starting with "@/..." to the root of the project ("./*")
      "@/*": ["./*"] 
    },
    // ... (rest of your compiler options)
  }
  // ... (rest of the file)
}