rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access so anyone can stream videos
      allow read;
      // Only allow writes if the user is authenticated
      allow write: if request.auth != null;
    }
  }
}
